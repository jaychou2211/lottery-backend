Meta Graph API 留言擷取系統：深度架構分析與實作指南1. 執行摘要與研究範疇本研究報告旨在回應並解決開發者針對 Meta (Facebook) Graph API 留言擷取機制的實作需求。核心目標為設計一套本地端執行的 Python 腳本系統，不僅需具備擷取貼文 (Post) 留言的基本功能，更需應對生產環境中的複雜挑戰，包括但不限於嚴格的權限驗證 (Authentication)、動態的速率限制 (Rate Limiting)、大數據量的分頁處理 (Pagination) 以及數據結構的完整性保存。鑑於 Graph API 的龐大體系與 v20.0 版本後的重大變革，單一檔案的腳本設計已無法滿足現代軟體工程對於「關注點分離 (Separation of Concerns)」與「可維護性 (Maintainability)」的要求。因此，本報告提出並實作了一種模組化架構 (Modular Architecture)，將系統拆解為配置管理、認證授權、網路傳輸核心、業務邏輯與數據持久化等五大獨立模組。本報告不僅提供程式碼實作，更將深入剖析 Meta 社交圖譜 (Social Graph) 的底層運作邏輯，探討從短效權杖交換至長效權杖的資安機制，並針對 API 回應中的游標分頁策略進行演算法層級的分析。此外，針對 stream 過濾器在近期版本中的棄用趨勢，本報告將論證採用「遞迴式擷取 (Recursive Fetching)」策略的必要性，確保系統在面對未來 API 變更時的魯棒性 (Robustness)。2. Meta Graph API 生態系統與架構原理Meta 的 Graph API 是網際網路上最複雜且規模最大的 HTTP 介面之一，其設計哲學基於「社交圖譜」概念，將平台上的所有資料視為節點 (Nodes) 與邊 (Edges) 的集合 1。理解此架構是開發穩定留言擷取系統的前提。2.1 節點、邊與欄位模型在 Graph API 的語境中，資料結構並非傳統關聯式資料庫的表格，而是圖論中的物件：節點 (Nodes)：代表單一實體，如使用者 (User)、粉絲專頁 (Page)、貼文 (Post) 或留言 (Comment)。每個節點由唯一的 ID (Object ID) 標識。值得注意的是，為了保護隱私，Meta 實施了「應用程式範圍 ID (App-Scoped ID, ASID)」機制，意即同一使用者在不同 App 中會有不同的 ID 1。邊 (Edges)：代表節點之間的連結。例如，一則貼文下方的所有留言，在圖譜中表現為該 Post 節點連向多個 Comment 節點的集合，這條路徑稱為 /comments edge 2。欄位 (Fields)：節點的屬性數據。例如 Comment 節點包含 message (內容)、created_time (時間戳)、from (作者) 等屬性。開發者必須明確指定所需欄位，否則 API 僅會回傳預設的最小集合 (通常僅有 ID) 1。2.2 API 版本控制與生命週期Meta 採用嚴格的版本控制策略 (例如 v19.0, v20.0)。每個版本通常有 2 年的生命週期。本報告的實作基於 v20.0+ 環境，這至關重要，因為舊版 API 中的某些功能 (如 FQL 或無限制的公開存取) 已被完全移除。特別是針對留言的 stream 過濾器，在 v20.0 及後續版本中，其行為模式已發生改變或被標記為過時，這直接影響了我們選擇「遞迴擷取」而非「扁平列表」的架構決策 3。2.3 HTTP 協議與資料交換格式Graph API 嚴格遵守 RESTful 風格，使用 HTTP GET 方法進行資料讀取。所有回應均為 JSON 格式。這意味著我們的本地實作腳本必須具備強大的 JSON 解析能力以及處理 HTTP 狀態碼 (Status Codes) 的邏輯。例如，200 OK 代表成功，而 400 Bad Request 或 500 Internal Server Error 則攜帶了具體的錯誤子代碼 (Subcodes)，這對於除錯與自動化重試至關重要 7。3. 認證與授權機制深度解析安全性是 Meta 平台的首要考量。任何對 API 的呼叫都必須附帶存取權杖 (Access Token)。對於留言擷取任務，選擇正確的權杖類型與權限範圍 (Scope) 是成功的關鍵。3.1 權杖類型及其適用場景在擷取留言的情境中，主要涉及三種權杖類型：權杖類型 (Token Type)描述與用途適用性分析User Access Token代表單一使用者。權限受限於該使用者的隱私設定與好友關係。僅適用於測試或擷取使用者自身貼文。在讀取公開專頁時，可能因隱私限制無法獲取完整資料 3。Page Access Token代表粉絲專頁管理員。擁有該專頁內容的完整管理權限 (讀取/隱藏/刪除留言)。最佳選擇。若目標是管理或分析自己專頁的留言，此權杖能提供最詳盡的數據 (如留言者資訊) 4。App Access Token代表應用程式本身。用於讀取公開數據。受限嚴重。自 2018 年劍橋分析事件後，Meta 大幅限縮了 App Token 讀取公開數據的能力 (Page Public Content Access) 3。3.2 權限範圍 (Permissions Scope)為了讀取貼文留言，應用程式必須在登入流程中請求特定權限。根據官方文件 4，以下權限是不可或缺的：pages_read_engagement：這是核心權限，允許 App 讀取粉絲專頁上的內容互動，包括留言、貼文與按讚。pages_manage_engagement：若腳本未來需擴充至「隱藏」或「回覆」留言，則需此權限。public_profile：基礎權限，用於讀取留言者的公開名稱與頭貼。read_insights：若需進一步分析貼文的成效數據 (如觸及率)，則需此權限 10。3.3 長效權杖交換機制 (Token Exchange)透過 Facebook Login 獲取的初始權杖通常為「短效權杖 (Short-lived Token)」，有效期僅約 1-2 小時。這對於需要長期運行的本地腳本是不可接受的。因此，系統必須實作「權杖交換」流程，將短效權杖換為有效期長達 60 天的「長效權杖 (Long-lived Token)」11。交換流程技術細節：這是一個伺服器對伺服器 (Server-to-Server) 的請求，不應在客戶端暴露 App Secret。但在本地腳本情境下，我們模擬伺服器行為。請求端點：GET /oauth/access_token必要參數：grant_type: 固定為 fb_exchange_tokenclient_id: App IDclient_secret: App Secret (高度敏感，需妥善保管)fb_exchange_token: 現有的短效權杖回應範例 11：JSON{
  "access_token": "EAAD...",
  "token_type": "bearer",
  "expires_in": 5183944  // 約 60 天
}
本報告的 auth.py 模組將完整實作此邏輯，並包含自動檢查權杖有效期的功能。4. 數據擷取策略與分頁演算法4.1 留言物件結構分析一個標準的 Comment Node 包含豐富的元數據。為了建立有意義的數據集，我們的腳本將請求以下關鍵欄位 4：id: 唯一識別碼，格式通常為 PostID_CommentID。message: 留言純文字內容。from: 發布者物件 (包含 Name 與 ID)。created_time: ISO 8601 格式的時間戳。like_count: 按讚數，反映留言的熱門程度。comment_count: 關鍵欄位，指示該留言下有多少則回覆 (Replies)。attachment: 圖片或影片連結。4.2 遞迴擷取 vs. 串流過濾 (Recursion vs. Stream)在舊版 API 中，開發者常使用 filter=stream 參數來一次性獲取所有層級的留言 (扁平化結構)。然而，根據最新文件與社群反饋 3，stream 過濾器在 v11.0 版本後對於某些權限與節點類型的支援度降低，且在「新版粉絲專頁體驗」中可能失效，導致僅回傳頂層留言或空數據。為了確保數據的完整性與系統的長期穩定性，本實作採用 「深度優先遞迴 (Depth-First Recursion)」 策略：第一層 (Top-level)：請求 /{object-id}/comments?filter=toplevel。遍歷檢查：針對回傳的每一則留言，檢查 comment_count 欄位。第二層 (Replies)：若 comment_count > 0，則以此留言 ID 為節點，再次發起請求 /{comment-id}/comments。結構重組：將獲取的回覆嵌入父留言的 replies 欄位中，形成巢狀結構 (Nested JSON)，或在匯出時展開為關聯式結構 (Flat CSV with Parent ID)。此策略雖然會增加 API 呼叫次數 (API Calls)，但能保證數據結構的正確性，並完全符合 Graph API v20+ 的設計規範。4.3 游標分頁 (Cursor-based Pagination)面對單一貼文可能包含數萬則留言的情況，分頁處理是不可或缺的。Graph API 提供多種分頁方式，但對於留言這類即時動態數據，游標分頁是唯一推薦的方案 12。為什麼不使用 Offset?Offset 分頁 (如 SQL 中的 LIMIT 10 OFFSET 100) 在數據變動時 (例如在讀取第 1 頁時有人刪除了留言) 會導致數據偏移，造成漏讀或重複讀取。游標分頁運作機制：API 回應中的 paging 物件包含 cursors (before/after 指針) 與 next (下一頁的完整 URL)。JSON"paging": {
  "cursors": {
    "after": "MTAxNTExOTQ1MjAwNzI5NDE=",
    "before": "NDMyNzQyODI3OTQw"
  },
  "next": "https://graph.facebook.com/v20.0/...?access_token=...&after=MTAx..."
}
實作邏輯必須是一個 while 迴圈：只要 paging.next 存在，就提取該 URL 繼續請求，直到 next 欄位消失。本報告的 fb_client.py 將封裝此邏輯，對呼叫者透明化。5. 韌性設計：速率限制與錯誤處理在分佈式系統中，網路是不穩定的，且 API 提供者會實施速率限制。腳本必須具備「韌性 (Resilience)」。5.1 速率限制模型 (Rate Limiting)Meta 的速率限制極為複雜，分為多個層級 7：平台層級 (Platform Rate Limits)：基於應用程式的用戶規模。公式通常為：200 * 用戶數 / 小時。商業用例層級 (Business Use Case Limits)：針對廣告帳戶或專頁的高頻操作。標頭分析 (Header Analysis)：API 回應標頭中會包含 X-App-Usage 或 X-Business-Use-Case-Usage。其值為 JSON 字串，例如：{"call_count": 85, "total_time": 10, "total_cputime": 20}這表示已消耗了 85% 的配額。當任一數值達到 100，API 將回傳錯誤代碼 4, 17, 32 或 613。5.2 指數退避演算法 (Exponential Backoff)當遇到限流錯誤時，立即重試通常會失敗並加劇限制。標準解法是實作指數退避 14：$$WaitTime = Base \times 2^{RetryCount}$$其中 $Base$ 為基礎等待時間 (如 1 秒)。若第一次重試失敗，等 2 秒；再失敗，等 4 秒，依此類推，直到達到最大重試次數。此外，建議加入隨機抖動 (Jitter) 以避免多個執行緒同時重試造成的共振效應，但對於單執行緒腳本，標準指數退避已足夠。5.3 錯誤處理矩陣腳本需區分「暫時性錯誤」與「永久性錯誤」：錯誤代碼 (Code)錯誤類型處理策略200成功解析 JSON 並處理數據。4, 17, 32, 613速率限制 (Rate Limit)暫時性。啟動指數退避機制，暫停後重試 7。500, 502, 503伺服器錯誤暫時性。Meta 伺服器偶發故障，可進行有限次數的重試。100, 102, 190權杖無效/過期永久性。Token 失效，重試無效。應記錄錯誤並終止程式，通知使用者重新授權 11。Other 4xx客戶端錯誤 (參數錯誤等)永久性。檢查程式邏輯或參數設定。6. 模組化實作腳本詳解基於上述架構分析，以下提供完整的 Python 實作。專案結構設計如下，以滿足「切成數個檔案」的需求，確保高內聚低耦合。檔案結構：config.py: 配置中心。utils.py: 共用工具 (日誌、時間)。fb_client.py: 封裝 HTTP 與重試邏輯。auth.py: 權杖管理。comment_fetcher.py: 遞迴擷取邏輯。data_processor.py: 數據清洗與匯出。main.py: 程式入口。6.1 config.py - 配置管理模組此模組負責集中管理環境變數與參數，避免敏感資訊硬編碼 (Hardcoding)。Python# config.py
import os

# ==========================================
# 敏感資訊配置
# 建議在生產環境中使用環境變數 (os.getenv)
# ==========================================
APP_ID = os.getenv('FB_APP_ID', 'YOUR_APP_ID_HERE')
APP_SECRET = os.getenv('FB_APP_SECRET', 'YOUR_APP_SECRET_HERE')
# 初始存取權杖 (User Token 或 Page Token)
ACCESS_TOKEN = os.getenv('FB_ACCESS_TOKEN', 'YOUR_SHORT_LIVED_TOKEN_HERE')

# ==========================================
# API 系統配置
# ==========================================
# Graph API 版本 - 鎖定版本以確保穩定性，避免 Meta 自動升級導致行為改變
API_VERSION = 'v20.0'
BASE_URL = f'https://graph.facebook.com/{API_VERSION}'

# 網路請求設定
TIMEOUT = 30  # 連線超時秒數
MAX_RETRIES = 5  # 最大重試次數
BACKOFF_FACTOR = 1.5  # 指數退避因子 (Wait = Factor ^ Retry * Base)
6.2 utils.py - 工具與日誌模組建立標準化的日誌系統 (Logging)，對於排查 API 錯誤至關重要。Python# utils.py
import logging
import sys

def setup_logger(name="MetaScraper"):
    """
    配置並回傳一個標準化的 Logger 實例。
    輸出格式包含時間、模組名稱與訊息等級，便於追蹤長時任務的執行狀況。
    """
    logger = logging.getLogger(name)
    
    # 若 logger 已存在 handler 則不重複添加，避免重複輸出
    if not logger.handlers:
        logger.setLevel(logging.INFO)
        
        # 輸出至控制台
        console_handler = logging.StreamHandler(sys.stdout)
        formatter = logging.Formatter(
            '[%(asctime)s][%(levelname)s][%(module)s]: %(message)s',
            datefmt='%Y-%m-%d %H:%M:%S'
        )
        console_handler.setFormatter(formatter)
        logger.addHandler(console_handler)
    
    return logger

# 全域共用的 logger 實例
logger = setup_logger()
6.3 fb_client.py - 核心網路客戶端模組此模組是系統的心臟，封裝了 requests 庫，處理 Session 管理、URL 構建、以及最關鍵的速率限制處理與自動重試。Python# fb_client.py
import time
import requests
import json
from config import BASE_URL, TIMEOUT, MAX_RETRIES, BACKOFF_FACTOR
from utils import logger

class FacebookClient:
    """
    負責處理與 Meta Graph API 的所有 HTTP 交互。
    包含：Session 管理、速率限制監控、錯誤處理與重試機制。
    """
    def __init__(self, access_token):
        self.access_token = access_token
        # 使用 Session 物件可重用 TCP 連線 (Keep-Alive)，顯著提升大量請求時的效能
        self.session = requests.Session()
    
    def _check_rate_limit_header(self, headers):
        """
        解析 X-App-Usage 標頭，主動監控配額使用量。
        """
        usage = headers.get('X-App-Usage')
        if usage:
            try:
                # header 值為 JSON 字串: {'call_count': 85, 'total_time': 10...}
                usage_data = json.loads(usage)
                max_usage = max(usage_data.values()) if usage_data else 0
                
                if max_usage > 90:
                    logger.warning(f"⚠️ API 配額使用率極高 ({max_usage}%)，系統將強制冷卻...")
                    time.sleep(10)  # 主動節流
                elif max_usage > 70:
                    logger.info(f"API 配額使用率: {max_usage}%")
            except Exception:
                pass  # 解析失敗不影響主流程

    def get(self, endpoint, params=None):
        """
        執行 GET 請求。
        
        Args:
            endpoint (str): API 路徑 (如 'me/accounts') 或完整分頁 URL。
            params (dict): 查詢參數。
            
        Returns:
            dict: 解析後的 JSON 數據，若失敗則回傳 None。
        """
        if params is None:
            params = {}
            
        # 判斷是否為完整 URL (處理分頁 next link)
        if endpoint.startswith('http'):
            url = endpoint
        else:
            # 移除開頭斜線，避免路徑錯誤
            url = f"{BASE_URL}/{endpoint.lstrip('/')}"
            # 注入 Access Token
            params['access_token'] = self.access_token

        retry_count = 0
        while retry_count < MAX_RETRIES:
            try:
                response = self.session.get(url, params=params, timeout=TIMEOUT)
                
                # 監控速率限制標頭
                self._check_rate_limit_header(response.headers)
                
                # 處理 HTTP 200 OK
                if response.status_code == 200:
                    return response.json()
                
                # 解析錯誤回應
                try:
                    error_json = response.json()
                    error_data = error_json.get('error', {})
                    code = error_data.get('code')
                    subcode = error_data.get('error_subcode')
                    message = error_data.get('message')
                except ValueError:
                    code, message = -1, "無法解析錯誤 JSON"

                logger.error(f"API 請求失敗 Code: {code}, Msg: {message}")

                # 策略性重試邏輯
                # 代碼 4, 17, 32, 613 為速率限制相關錯誤
                # 代碼 5xx 為伺服器端錯誤
                if code in  or response.status_code >= 500:
                    wait_time = 5 * (BACKOFF_FACTOR ** retry_count)
                    logger.warning(f"觸發限流或伺服器錯誤，等待 {wait_time:.1f} 秒後重試 ({retry_count+1}/{MAX_RETRIES})...")
                    time.sleep(wait_time)
                    retry_count += 1
                    continue
                
                # 其他錯誤 (如 190 Token 過期, 100 參數錯誤) 視為永久性錯誤，不重試
                return None

            except requests.exceptions.RequestException as e:
                # 處理網路層級錯誤 (DNS 解析失敗, 連線中斷)
                wait_time = 2 * (BACKOFF_FACTOR ** retry_count)
                logger.error(f"網路連線異常: {e}，等待重試...")
                time.sleep(wait_time)
                retry_count += 1
        
        logger.critical("已達到最大重試次數，放棄此請求。")
        return None
6.4 auth.py - 權杖認證模組實作從短效 Token 交換長效 Token 的標準流程。Python# auth.py
from config import APP_ID, APP_SECRET
from fb_client import FacebookClient
from utils import logger

def exchange_token(short_token):
    """
    將短效 User/Page Token 交換為長效 Token (60天)。
    """
    if not APP_ID or not APP_SECRET:
        logger.warning("未設定 APP_ID 或 APP_SECRET，跳過長效權杖交換，使用原 Token。")
        return short_token

    logger.info("正在嘗試交換長效權杖...")
    
    # 使用臨時 Client 進行交換請求
    # 注意：這裡不需帶入 access_token 到 header，而是作為參數
    client = FacebookClient(access_token=None) 
    
    params = {
        'grant_type': 'fb_exchange_token',
        'client_id': APP_ID,
        'client_secret': APP_SECRET,
        'fb_exchange_token': short_token
    }
    
    # 呼叫 oauth/access_token 端點
    response = client.get('oauth/access_token', params=params)
    
    if response and 'access_token' in response:
        long_token = response['access_token']
        expires_in = response.get('expires_in')
        logger.info(f"長效權杖獲取成功！有效期約 {int(expires_in/86400)} 天。")
        return long_token
    else:
        logger.error("長效權杖交換失敗，將繼續使用原始權杖。")
        return short_token
6.5 comment_fetcher.py - 業務邏輯模組本模組實作核心的「遞迴擷取」與「游標分頁」邏輯。Python# comment_fetcher.py
from fb_client import FacebookClient
from utils import logger

class CommentFetcher:
    def __init__(self, client: FacebookClient):
        self.client = client
        # 定義所需的欄位 (Fields Projection)，減少傳輸數據量
        # 包含作者資訊、按讚數、回覆數、附件(圖片)
        self.fields = (
            'id,message,created_time,like_count,comment_count,'
            'from{id,name},attachment'
        )

    def fetch_post_comments(self, object_id):
        """
        主函式：擷取指定 Post/Object 的所有留言 (含回覆)。
        """
        logger.info(f"開始擷取物件 {object_id} 的留言...")
        all_comments =
        
        # 初始請求：獲取頂層留言 (Toplevel)
        # order='reverse_chronological' 確保先獲取最新留言
        params = {
            'fields': self.fields,
            'limit': 100,  # 建議設為最大值 100 以減少請求次數
            'filter': 'toplevel', 
            'order': 'reverse_chronological'
        }
        
        endpoint = f"{object_id}/comments"
        
        # 執行分頁遍歷 (Pagination Loop)
        top_level_data = self._paginate(endpoint, params)
        
        logger.info(f"共獲取 {len(top_level_data)} 則頂層留言，開始掃描回覆...")
        
        # 處理每一則頂層留言，檢查是否有回覆
        for index, comment in enumerate(top_level_data):
            if index % 50 == 0 and index > 0:
                logger.info(f"進度: 已處理 {index}/{len(top_level_data)} 則留言...")

            # 遞迴觸發條件：comment_count > 0
            reply_count = comment.get('comment_count', 0)
            if reply_count > 0:
                # 擷取該留言的子回覆
                replies = self._fetch_replies(comment['id'])
                comment['replies'] = replies  # 將回覆嵌入父物件
            else:
                comment['replies'] =
            
            all_comments.append(comment)
            
        return all_comments

    def _fetch_replies(self, comment_id):
        """
        輔助函式：針對特定留言 ID 擷取其子回覆。
        """
        params = {
            'fields': self.fields,
            'limit': 100  # 回覆通常較少，但仍需設高
        }
        endpoint = f"{comment_id}/comments"
        return self._paginate(endpoint, params)

    def _paginate(self, endpoint, params):
        """
        通用的游標分頁處理器。
        自動處理 paging.next 直到沒有下一頁。
        """
        results =
        current_url = None
        
        while True:
            if current_url:
                # 使用完整 URL (next link) 請求
                response = self.client.get(current_url)
            else:
                # 初始請求
                response = self.client.get(endpoint, params=params)
            
            if not response or 'data' not in response:
                break
                
            data = response['data']
            if not data:
                break
                
            results.extend(data)
            
            # 檢查是否有下一頁 (paging.next)
            paging = response.get('paging', {})
            next_url = paging.get('next')
            
            if next_url:
                current_url = next_url
            else:
                break  # 終止分頁
        
        return results
6.6 data_processor.py - 數據處理模組負責將複雜的巢狀 JSON 轉換為便於分析的 CSV 格式，或保存原始 JSON。Python# data_processor.py
import json
import csv
from utils import logger

def save_json(data, filename):
    """保存為 JSON 格式，保留完整的巢狀結構。"""
    try:
        with open(filename, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
        logger.info(f"數據已保存至: {filename}")
    except IOError as e:
        logger.error(f"寫入 JSON 失敗: {e}")

def save_csv(data, filename):
    """
    將巢狀數據展平 (Flatten) 並保存為 CSV。
    每列代表一則留言，透過 type 和 parent_id 區分層級。
    """
    flat_rows =
    
    for comment in data:
        # 1. 處理頂層留言
        parent_row = _parse_comment(comment, is_reply=False)
        flat_rows.append(parent_row)
        
        # 2. 處理該留言的所有回覆
        for reply in comment.get('replies',):
            reply_row = _parse_comment(reply, is_reply=True, parent_id=comment['id'])
            flat_rows.append(reply_row)
            
    if not flat_rows:
        logger.warning("無數據可寫入 CSV。")
        return

    # 定義 CSV 欄位順序
    headers = [
        'id', 'type', 'parent_id', 'created_time', 
        'author_name', 'author_id', 'message', 
        'like_count', 'comment_count', 'attachment_url'
    ]
    
    try:
        with open(filename, 'w', encoding='utf-8', newline='') as f:
            writer = csv.DictWriter(f, fieldnames=headers)
            writer.writeheader()
            writer.writerows(flat_rows)
        logger.info(f"數據已保存至: {filename} (共 {len(flat_rows)} 筆)")
    except IOError as e:
        logger.error(f"寫入 CSV 失敗: {e}")

def _parse_comment(comment_obj, is_reply=False, parent_id=None):
    """輔助函式：提取單一留言的欄位值"""
    author = comment_obj.get('from', {})
    
    # 提取附件連結 (若有)
    attachment = comment_obj.get('attachment', {})
    media = attachment.get('media', {})
    image_src = media.get('image', {}).get('src', '')
    target_url = attachment.get('target', {}).get('url', '')
    attach_url = image_src or target_url

    return {
        'id': comment_obj.get('id'),
        'type': 'reply' if is_reply else 'toplevel',
        'parent_id': parent_id,
        'created_time': comment_obj.get('created_time'),
        'author_name': author.get('name', 'Unknown'),
        'author_id': author.get('id', ''),
        'message': comment_obj.get('message', ''),
        'like_count': comment_obj.get('like_count', 0),
        'comment_count': comment_obj.get('comment_count', 0),
        'attachment_url': attach_url
    }
6.7 main.py - 主程式入口整合所有模組，執行完整的 ETL (Extract, Transform, Load) 流程。Python# main.py
import sys
from config import ACCESS_TOKEN
from auth import exchange_token
from fb_client import FacebookClient
from comment_fetcher import CommentFetcher
from data_processor import save_json, save_csv
from utils import logger

def main():
    logger.info("=== Meta Graph API Comment Scraper v2.0 ===")
    
    # 1. 檢查基礎配置
    if not ACCESS_TOKEN:
        logger.error("錯誤：未檢測到 Access Token。請在 config.py 或環境變數中設定。")
        sys.exit(1)
        
    # 2. 獲取使用者輸入
    target_id = input("請輸入目標 Post ID (或 Object ID): ").strip()
    if not target_id:
        logger.error("ID 不能為空。")
        sys.exit(1)

    # 3. 執行權杖交換 (Short -> Long)
    # 這一步確保我們有足夠的時間執行長任務
    final_token = exchange_token(ACCESS_TOKEN)
    
    # 4. 初始化客戶端與邏輯控制器
    client = FacebookClient(final_token)
    
    # 測試 Token 有效性
    logger.info("驗證 Token 權限...")
    me = client.get('me', params={'fields': 'id,name'})
    if not me:
        logger.error("Token 驗證失敗 (可能過期或權限不足)，程式終止。")
        sys.exit(1)
    logger.info(f"當前操作身分: {me.get('name')} (ID: {me.get('id')})")

    # 5. 執行擷取任務
    fetcher = CommentFetcher(client)
    try:
        comments_data = fetcher.fetch_post_comments(target_id)
    except KeyboardInterrupt:
        logger.warning("使用者中斷程式。正在嘗試保存已獲取的數據...")
        sys.exit(0)

    # 6. 輸出結果
    if comments_data:
        logger.info("正在匯出檔案...")
        save_json(comments_data, f"comments_{target_id}.json")
        save_csv(comments_data, f"comments_{target_id}.csv")
        logger.info("任務圓滿完成。")
    else:
        logger.warning("未獲取到任何數據 (可能是權限不足或該貼文無留言)。")

if __name__ == "__main__":
    main()
7. 進階優化與未來展望雖然上述腳本已能滿足大多數本地擷取需求，但在處理百萬級別留言的超大型專頁時，仍有進一步優化的空間。7.1 非同步 I/O (Asynchronous I/O)Python 的 requests 庫是同步阻塞的 (Blocking)，這意味著 CPU 在等待網路回應時是閒置的。對於遞迴擷取回覆的場景，我們可以利用 Python 的 asyncio 配合 aiohttp 庫來併發處理請求。架構調整建議：將 CommentFetcher 改寫為 Async Class。使用 asyncio.gather 同時發出多個頂層留言的回覆查詢請求。警示：並發請求極易瞬間觸發速率限制。必須引入 asyncio.Semaphore (信號量) 來限制同時發出的請求數量 (例如最大併發數設為 5)，並配合「權杖桶演算法 (Token Bucket)」進行客戶端限流。7.2 批次請求 (Batch Requests)Graph API 支援批次請求功能 16，允許將多個 API 呼叫 (最多 50 個) 打包成單一 HTTP POST 請求。POST /?batch=這能顯著降低 HTTP 握手 (Handshake) 的開銷，特別是在擷取大量子回覆時。然而，批次請求的錯誤處理較為複雜，因為即使 HTTP 狀態碼為 200，批次中的個別請求仍可能失敗，需要逐一解析。7.3 Webhooks 與實時更新對於需要持續監控留言的應用，輪詢 (Polling) 並非最佳解。Meta 提供 Webhooks 機制，當有新留言產生時，伺服器會主動發送 POST 請求通知我們。這雖然超出了「本地腳本」的範疇 (需要一個具備公網 IP 的伺服器)，但對於生產級別的監控系統而言，這是從「主動拉取」轉向「事件驅動」架構的必經之路。8. 結論本報告提供了一套完整的 Meta Graph API 留言擷取解決方案。透過模組化設計，我們解決了單一腳本難以維護的問題；透過遞迴擷取策略，我們規避了 API 過濾器的不確定性；透過指數退避與長效權杖交換，我們確保了系統在惡劣網路環境與長時間運行下的穩定性。開發者在使用此系統時，應始終關注 Meta 開發者政策的變動，特別是關於用戶數據隱私 (GDPR/CCPA) 的規範，確保在獲取數據的便利性與合規性之間取得平衡。此腳本不僅是一個工具，更是理解現代大型社交網路 API 架構的實踐範本。