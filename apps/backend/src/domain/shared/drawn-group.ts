/**
 * Indicates which group the winner was drawn from
 *
 * - SENIOR: drawn from senior pool
 * - JUNIOR: drawn from junior pool
 * - ALL: drawn from combined pool (bonus prizes)
 */
export enum DrawnGroup {
	SENIOR = 'SENIOR',
	JUNIOR = 'JUNIOR',
	ALL = 'ALL',
}
